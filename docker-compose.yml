version: "3"

services:
  bank-job:
    build:
      context: .
      dockerfile: Dockerfile
    image: nodejs
    container_name: bank-jobs
    restart: always
    env_file: .env
    environment:
      - NODE_ENV=${NODE_ENV}
      - NUXT_HOST=${NUXT_HOST}
      - NUXT_PORT=${NUXT_PORT}
      - MONGO_HOST=${MONGO_HOST}
      - MONGO_PORT=${MONGO_PORT}
      - MONGO_DB=${MONGO_DB}
      - MONGO_RETRIES=${MONGO_RETRIES}
    depends_on:
      - bank-job-db
    ports:
      - "3000:3000"
    volumes:
      - .:/home/node/app
      - node_modules:/home/node/app/node_modules
    networks:
      - bank-job-network
    command: ./db.sh db:27017 -- npm run dev

  bank-job-db:
    image: mongo:4.1.8-xenial
    container_name: db
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - dbdata:/data/db
    networks:
      - bank-job-network

networks:
  bank-job-network:
    driver: bridge

volumes:
  dbdata:
  node_modules:

